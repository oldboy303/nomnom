const app = require('../app.js');
const request = require('supertest');
const Cookbook = require('../models/cookbook.js');

describe('Registration/create tests', () => {

  it('It should save a new cookbook to the DB');

});
